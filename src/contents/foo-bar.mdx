---
category: notes
tags:
    - Next.js
    - SEO
title: 測試文章2的標題
description: 這是一篇測試用的文章2
date: 2025-02-26T08:34:00Z
---

# 測試文章的標題

這是一篇測試用的文章

![測試圖片](/images/ow_slicing_practice.png)

```typescript
function newLineToSpace(str: string): string {
    return str.replace(/\s+/g, " ");
}

function classNames(...arg: any[]): string {
    const stringArray = [];
    for (let i = 0; i < arg.length; i++) {
        const item = arg[i];
        if (typeof item === "string" && item.trim() !== "") {
            stringArray.push(item.trim());
        } else if (Array.isArray(item)) {
            if (item.length) {
                stringArray.push(classNames(...item));
            }
        } else if (typeof item === "object" && item.toString === Object.prototype.toString) {
            for (const key in item) {
                if (item[key] && key.trim() !== "") {
                    stringArray.push(key.trim());
                }
            }
        }
    }
    return newLineToSpace(stringArray.join(" "));
}

export default classNames;
```

- 測試列表1
- 測試列表2
- 測試列表3

| 名稱 | 價格 | 庫存 |
|------|------|------|
| 蘋果 | $2   | 100  |
| 香蕉 | $1   | 50   |
| 橘子 | $3   | 20   |

<div>
    <p>這是一段測試用的文字</p>
</div>

<div>
    <p>這是一段測試用的文字</p>
</div>

這是一段 ~~刪除的文字~~。

- [x] 已完成項目
- [ ] 待完成項目