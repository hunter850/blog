import { Fragment } from "react";
// components

async function About(): Promise<React.JSX.Element> {
    return (
        <Fragment>
            <h1>About</h1>
        </Fragment>
    );
}

export default About;
